pipeline {
    agent any
    stages {
        stage('hello') {
            steps {
                git branch: 'main', url: 'https://github.com/kirannunnak/GYMONE.git', credentialsId: 'gitcredentials'
            }
        }
    }
}
